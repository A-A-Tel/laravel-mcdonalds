# Project

## Run deze commands vanaf de repo root en je kunt op de website komen.

```shell
  cp .env.example .env
  touch database/database.sqlite
  composer install --no-dev
  php artisan key:generate
  php artisan key:generate
  php artisan migrate
  php artisan db:seed
  php artisan serve
```
